# torch is installed separately (CPU wheel) in the workflow.
numpy<3
beautifulsoup4
# Trackformer1.1 reads GFS f000 analysis GRIB2. The eccodes wheel carries its own ecCodes
# binaries, so the runner needs no apt package.
xarray
cfgrib
eccodes
# scipy is NOT a hard xarray dependency, but DataArray.interp() -- how the
# storm-centred 17x17 patches are sampled -- refuses to run without it. Missing
# it only surfaced in CI, where the Trackformer1.1 route fell back to Trackformer1.0 rather than
# failing loudly.
scipy
# predict_ibtracs_jma_only (the window builder the v37G head was validated with)
# imports pyplot at module level; MPLBACKEND=Agg keeps it headless.
matplotlib
# Trackformer 1.2's constructor loads its ocean-structure calibration through
# joblib and raises ImportError without it -- even though this job never calls
# predict_ocean_structure, the model cannot be constructed at all.
#
# scikit-learn comes with it: the joblib artifact is a pickled sklearn estimator,
# so unpickling imports sklearn. The module catches ImportError broadly and
# reports "joblib is required", which sent me after the wrong package once --
# the real error underneath was ModuleNotFoundError: No module named 'sklearn'.
joblib
scikit-learn
